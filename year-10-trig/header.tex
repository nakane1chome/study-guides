% Custom LaTeX header for better table formatting

% Increase table row height (default is 1.0)
\renewcommand{\arraystretch}{1.5}

% Add some space around table cells
\usepackage{array}
\setlength{\tabcolsep}{12pt}

% Optional: Make tables look nicer with booktabs
\usepackage{booktabs}
